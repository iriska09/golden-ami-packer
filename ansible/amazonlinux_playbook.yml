---
- name: CIS Level 1 Hardening - Amazon Linux 2023
  hosts: all
  become: true

  vars:
    sshd_config_path: /etc/ssh/sshd_config

  tasks:
    - name: Disable root login via SSH
      lineinfile:
        path: "{{ sshd_config_path }}"
        regexp: '^PermitRootLogin'
        line: 'PermitRootLogin no'

    - name: Install auditd for system monitoring (Force correct package manager)
      ansible.builtin.dnf:
        name: auditd
        state: present
        use_backend: dnf5  # 🚀 **Explicitly specify dnf5 backend to avoid detection issues**

    - name: Enable auditd
      ansible.builtin.service:
        name: auditd
        enabled: true
        state: started

    - name: Install cronie (for security updates)
      ansible.builtin.dnf:
        name: cronie
        state: present
        use_backend: dnf5  # 🚀 **Ensures dnf5 is used for package installation**

    - name: Configure automatic security updates
      ansible.builtin.cron:
        name: "Automatic security updates"
        minute: "0"
        hour: "2"
        job: "dnf -y update --security"
